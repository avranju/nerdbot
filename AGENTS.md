# Nerdbot — Development Rules

- **Do NOT commit changes to git automatically.** Only commit when the user
  explicitly asks for a commit or when I am explicitly instructed to "commit the
  changes."
- Wait for the user to ask before running git commit or git add.
